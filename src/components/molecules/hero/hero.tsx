// import React from 'react'

// // import '@/styles/hero.css'

// const Hero = React.forwardRef<HTMLDivElement, any>(
//   ({ className, ...props }, ref) => {
//     return (
//       <section
//         className={`hero content-spacing ${className}`}
//         role='banner'
//         ref={ref}
//         {...props}
//       >
//         {props.children}
//       </section>
//     )
//   }
// )

// Hero.displayName = 'Hero'

// const HeroImageContainer = React.forwardRef<HTMLDivElement, any>(
//   ({ className, ...props }, ref) => {
//     return (
//       <div className={`hero-image-container ${className}`} ref={ref} {...props}>
//         {props.children}
//       </div>
//     )
//   }
// )

// HeroImageContainer.displayName = 'HeroImageContainer'

// const HeroTextContainer = React.forwardRef<HTMLDivElement, any>(
//   ({ className, ...props }, ref) => {
//     return (
//       <div className={`hero-text-container ${className}`} ref={ref} {...props}>
//         {props.children}
//       </div>
//     )
//   }
// )

// HeroTextContainer.displayName = 'HeroTextContainer'

// export { Hero, HeroImageContainer, HeroTextContainer }
